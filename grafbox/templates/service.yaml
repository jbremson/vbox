apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafbox.fullname" . }}
  labels:
    {{- include "grafbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafbox.selectorLabels" . | nindent 4 }}
